import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  header: {
    fontFamily: 'Inter',
    textAlign: 'center',
    fontWeight: '600',
    fontSize: 20,
    lineHeight: 25,
    marginBottom: 30,
    color: '#000',
  },
  saveContainer: {
    position: 'absolute',
    right: 0,
  },
  save: {
    fontFamily: 'Inter',
    fontStyle: 'normal',
    fontWeight: '600',
    fontSize: 17,
    color: '#3A6598',
  }
});

export default styles;